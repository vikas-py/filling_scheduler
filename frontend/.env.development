# Development Environment Variables

# API Base URL (adjust to your backend server)
# NOTE: Must include /api/v1 prefix as backend routes are under /api/v1/*
VITE_API_URL=http://192.168.56.101:8000/api/v1

# WebSocket URL (adjust to your backend server)
VITE_WS_URL=ws://192.168.56.101:8000/ws

# Disable WebSocket until backend WebSocket endpoint is implemented
# Set to 'true' when backend supports WebSocket
VITE_ENABLE_WEBSOCKET=false

# Enable debug logging in development
VITE_DEBUG=true
